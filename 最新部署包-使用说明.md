# MediaGenie 最新部署包使用说明

## �?**新部署包已创建！**

```
�?文件�? MediaGenie-Complete-Deploy.zip
�?大小: 0.41 MB
�?位置: F:\project\MediaGenie1001\MediaGenie-Complete-Deploy.zip
�?包含: 完整的前后端代码 + 3个部署脚�?```

---

## 📦 **包含内容**

```
MediaGenie-Complete-Deploy.zip
├── backend/
�?  └── media-service/          �?完整后端代码
├── frontend/
�?  └── build/                  �?生产构建 (1.26 MB)
├── azuredeploy.json            �?ARM 模板 (默认 F1)
├── deploy-now.sh               �?交互式部署脚本（推荐�?├── quick-deploy.sh             �?快速部署脚�?├── .deployment                 �?Kudu 配置
├── DEPLOYMENT_README.md        �?部署说明
├── Dockerfile
├── docker-compose.yml
└── README.md

总文件数: 23
```

---

## 🚀 **部署步骤�?种方法）**

### **方法1：交互式部署（最推荐�?*

#### **步骤1：上传并解压**

1. 打开 Azure Cloud Shell: https://shell.azure.com
2. 上传 `MediaGenie-Complete-Deploy.zip`
3. 解压�?   ```bash
   unzip MediaGenie-Complete-Deploy.zip -d mediagenie-deploy
   cd mediagenie-deploy
   ```

#### **步骤2：运行交互式部署脚本**

```bash
chmod +x deploy-now.sh
./deploy-now.sh
```

**脚本会引导你�?*
- �?输入 Azure OpenAI API Key
- �?输入 Azure OpenAI Endpoint
- �?输入 Azure Speech Key
- �?选择 SKU (F1=免费 �?S1=标准)
- �?自动创建资源�?- �?自动部署应用
- �?显示访问 URL

**预期时间**�?-10 分钟

---

### **方法2：快速部署（编辑后运行）**

#### **步骤1：编辑配�?*

```bash
nano quick-deploy.sh
```

**修改以下变量**�?```bash
OPENAI_KEY="your-openai-key-here"
OPENAI_ENDPOINT="https://your-openai.openai.azure.com/"
SPEECH_KEY="your-speech-key-here"
SPEECH_REGION="eastus"
SKU="F1"  # F1=Free, S1=Standard
```

#### **步骤2：运行部�?*

```bash
chmod +x quick-deploy.sh
./quick-deploy.sh
```

---

### **方法3：手�?ARM 模板部署**

#### **使用 Free Tier (F1) - 无配额问�?*

```bash
az group create --name MediaGenie-RG --location eastus

az deployment group create \
  --resource-group MediaGenie-RG \
  --template-file azuredeploy.json \
  --parameters \
    siteName=mediagenie-test-$(date +%s) \
    sku=F1 \
    azureOpenAIKey="your-openai-key" \
    azureOpenAIEndpoint="https://your-openai.openai.azure.com/" \
    azureSpeechKey="your-speech-key" \
    azureSpeechRegion="eastus"
```

#### **使用 Standard Tier (S1) - 生产环境**

```bash
az deployment group create \
  --resource-group MediaGenie-RG \
  --template-file azuredeploy.json \
  --parameters \
    siteName=mediagenie-prod-$(date +%s) \
    sku=S1 \
    azureOpenAIKey="your-openai-key" \
    azureOpenAIEndpoint="https://your-openai.openai.azure.com/" \
    azureSpeechKey="your-speech-key" \
    azureSpeechRegion="eastus"
```

---

## 🎯 **SKU 选择指南**

| SKU | 成本 | 配额问题 | 计算时间 | Always On | 推荐场景 |
|-----|------|---------|---------|-----------|---------|
| **F1 (Free)** | 免费 | �?�?| 60分钟/�?| �?| **测试、演�?* |
| **S1 (Standard)** | $70/�?| �?�?| 无限�?| �?| **生产环境** |
| **B1 (Basic)** | $13/�?| ⚠️ 需申请 | 无限�?| �?| 小型生产 |

**推荐**：先�?**F1** 测试，满意后升级�?**S1**�?
---

## 📋 **完整部署示例**

### **示例：使用交互式脚本**

```bash
# 1. 上传 MediaGenie-Complete-Deploy.zip �?Cloud Shell

# 2. 解压
unzip MediaGenie-Complete-Deploy.zip -d mediagenie-deploy
cd mediagenie-deploy

# 3. 查看文件
ls -la

# 输出应该包含:
# - deploy-now.sh
# - quick-deploy.sh
# - azuredeploy.json
# - backend/
# - frontend/

# 4. 运行交互式部�?chmod +x deploy-now.sh
./deploy-now.sh

# 5. 按提示输�?
# Azure OpenAI API Key: sk-xxxxx
# Azure OpenAI Endpoint: https://openai250922.openai.azure.com/
# Azure Speech Key: xxxxx
# Azure Speech Region [eastus]: eastus
# Choose SKU (F1=Free, S1=Standard) [F1]: F1

# 6. 等待部署完成 (5-10 分钟)

# 7. 访问应用
# https://mediagenie-xxxxx.azurewebsites.net/
```

---

## �?**部署后验�?*

### **访问应用**

部署完成后，你会看到�?
```
====================================================================
                    Deployment Complete!
====================================================================

Application Info:
  Name:           mediagenie-1234567890
  SKU:            F1
  Resource Group: MediaGenie-RG

Access URLs:
  Website:     https://mediagenie-1234567890.azurewebsites.net
  API Docs:    https://mediagenie-1234567890.azurewebsites.net/docs
  Health:      https://mediagenie-1234567890.azurewebsites.net/health
```

### **验证清单**

- [ ] 访问 Website URL，看�?MediaGenie 界面
- [ ] 访问 API Docs，看�?Swagger UI
- [ ] 访问 Health，返�?200 OK
- [ ] 测试文本转语音功�?- [ ] 测试 GPT 对话功能
- [ ] 测试语音转文本功�?- [ ] 测试图像分析功能

---

## 🔄 **升级 SKU**

### **�?F1 升级�?S1**

```bash
# 获取 App Service Plan 名称
az appservice plan list \
  --resource-group MediaGenie-RG \
  --query "[].{Name:name, SKU:sku.name}" \
  --output table

# 升级�?S1
az appservice plan update \
  --name mediagenie-xxxxx-plan \
  --resource-group MediaGenie-RG \
  --sku S1
```

**注意**：升级不会丢失数据！

---

## 🐛 **常见问题**

### **Q1：deploy-now.sh 找不到？**

**A**：确保你解压了新的部署包 `MediaGenie-Complete-Deploy.zip`，不是旧的�?
```bash
# 检查文�?ls -la

# 应该看到:
# deploy-now.sh
# quick-deploy.sh
# azuredeploy.json
```

### **Q2：权限被拒绝�?*

**A**：添加执行权限：

```bash
chmod +x deploy-now.sh
chmod +x quick-deploy.sh
```

### **Q3：还是遇到配额问题？**

**A**：确保使�?F1 �?S1 SKU�?
```bash
# �?deploy-now.sh 中选择 F1
# 或在 quick-deploy.sh 中设�?SKU="F1"
```

### **Q4：如何查看部署日志？**

**A**�?
```bash
# 实时日志
az webapp log tail \
  --name mediagenie-xxxxx \
  --resource-group MediaGenie-RG

# 下载日志
az webapp log download \
  --name mediagenie-xxxxx \
  --resource-group MediaGenie-RG
```

### **Q5：部署失败怎么办？**

**A**�?
1. 检�?API Keys 是否正确
2. 检�?Azure 订阅是否有效
3. 查看部署日志
4. 尝试使用不同�?SKU (F1 �?S1)

---

## 📊 **部署脚本对比**

| 脚本 | 特点 | 适用场景 |
|------|------|---------|
| **deploy-now.sh** | 交互式，引导输入 | 首次部署，不熟悉命令 |
| **quick-deploy.sh** | 编辑配置后运�?| 重复部署，快速测�?|
| **ARM 模板** | 完全手动控制 | 自定义配置，CI/CD |

---

## 🎉 **总结**

### **新部署包的优�?*

1. �?**包含 3 个部署脚�?*：交互式、快速、手�?2. �?**默认使用 F1 (Free)**：无配额问题
3. �?**完整的前后端代码**：一次部署全搞定
4. �?**详细的文�?*：DEPLOYMENT_README.md

### **推荐流程**

1. **上传** `MediaGenie-Complete-Deploy.zip` �?Cloud Shell
2. **解压** 并进入目�?3. **运行** `./deploy-now.sh`（推荐）
4. **验证** 功能是否正常
5. **升级** SKU（如需生产环境�?
---

## 🚀 **现在开始！**

### **快速命�?*

```bash
# 1. 上传 MediaGenie-Complete-Deploy.zip �?Cloud Shell

# 2. 解压
unzip MediaGenie-Complete-Deploy.zip -d mediagenie-deploy
cd mediagenie-deploy

# 3. 部署（推荐）
chmod +x deploy-now.sh
./deploy-now.sh

# 或使用快速部署（先编�?quick-deploy.sh�?chmod +x quick-deploy.sh
./quick-deploy.sh
```

---

**🎉 新部署包已准备就绪！现在就上传并部署吧！**

**📍 文件位置**：`F:\project\MediaGenie1001\MediaGenie-Complete-Deploy.zip`

