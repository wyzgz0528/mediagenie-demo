# MediaGenie 前端开发环境 Dockerfile
FROM node:16-alpine

# 设置工作目录
WORKDIR /app

# 设置环境变量
ENV NODE_ENV=development
ENV CHOKIDAR_USEPOLLING=true
ENV GENERATE_SOURCEMAP=true

# 安装系统依赖
RUN apk add --no-cache \
    git \
    python3 \
    make \
    g++

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "start"]
